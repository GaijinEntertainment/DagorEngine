Screen-space quad with texture coordinates, produced by GetPackedQuad.
Texture coordinate top (V).
Screen-space right edge.
Screen-space left edge.
Texture coordinate bottom (V).
Texture coordinate left (U).
Screen-space top edge.
Screen-space bottom edge.
Texture coordinate right (U).