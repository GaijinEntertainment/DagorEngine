properties of the `ExprMakeStruct` object.
Use initializer, i.e. 'Foo(...)', and not 'Foo(uninitialized ...)'.
Its 'new Foo(...)'.
'useInitializer' was used optimized out.
Generated class initializer.
Its 'new ClassName(...)'.
Its declared via 'class'syntax, so using it for regular types will fail.
Its declared via 'struct' syntax, so using it for regular types will fail.
Its declared via 'variant' syntax, so using it for regular types will fail.
Its declared via 'tuple' syntax, so using it for regular types will fail.
Always use initializer, even for default construction.
Ignores visibility check between modules.
'where' section argument can shadow other variables. This is for nested comprehensions and such.
