Array comprehension (`[for (x in 0..3); x]`, `[iterator for (y in range(100)); x*2; where (x!=13)]]` for arrays or generators accordingly).
Location of the expression in source code
Type of the expression
Runtime type information of the class of the expression (i.e "ExprConstant", "ExprCall", etc)
Expression generation flags
Expression flags
Expression print flags
The 'for' expression
The 'where' expression
The subexpression
If generator syntax is used (i.e. `[iterator for ...]` instead of `[for]`)
If table syntax is used (i.e. `{for ...}` instead of `[for]`)
