DOM node type identifying the kind of XML node.
Empty (null) node handle.
Document tree root (not an XML element).
Element tag (e.g. `<node>`).
Plain character data (text content).
Character data section (`<![CDATA[...]]>`).
Comment node (`<!-- ... -->`).
Processing instruction (`<?name ...?>`).
XML declaration (`<?xml ...?>`).
Document type declaration (`<!DOCTYPE ...>`).