Bitfield controlling ``debug_expression`` output formatting.
``refCount`` — include smart pointer reference counts in the dump.
``lineInfo`` — include source file and line:column spans on each AST node. For call expressions, also shows ``atEnclosure`` when set.