Lightweight descriptor for a block encountered during data walking.
Stack offset of the block.
Arguments offset on the stack.
Pointer to the block's simulation node (`SimNode`).
Pointer to the AOT-compiled function (if any).
Pointer to the block's function arguments.
Pointer to the block's `FuncInfo` metadata.
