Packed character data produced by PackFontRange. Contains atlas coordinates, screen offsets, and advance width.
Atlas left edge (in pixels).
Atlas top edge (in pixels).
Atlas right edge (in pixels).
Atlas bottom edge (in pixels).
Screen-space left offset from cursor.
Screen-space top offset from baseline.
Horizontal advance to next character.
Screen-space right offset from cursor.
Screen-space bottom offset from baseline.