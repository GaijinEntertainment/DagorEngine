Channel mixing strategy when converting between channel counts.
Rectangular panning (simple matrix mix).
Simple channel mapping (drop or duplicate channels).
Custom weight matrix.
Default mixing mode.
