If-then-else expression (`if expr1 {your; block; here;} else {your; block; here;}`) including `static_if`'s.
Location of the expression in source code
Type of the expression
Runtime type information of the class of the expression (i.e "ExprConstant", "ExprCall", etc)
Expression generation flags
Expression flags
Expression print flags
The condition expression
The 'then' block expression
The 'else' block expression
Flags specific to if-then-else expressions
