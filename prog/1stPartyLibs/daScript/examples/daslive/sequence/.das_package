options gen2
require daslib/daspkg

[export]
def package() {
    package_name("sequence")
    package_author("borisbat")
    package_description("Sequence board game — live coding project")
}

[export]
def dependencies(version : string) {
    require_package("das-cards")
}
