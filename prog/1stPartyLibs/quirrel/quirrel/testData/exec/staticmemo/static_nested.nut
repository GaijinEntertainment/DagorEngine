for (local x = 111; x < 114; x++)
  println(static static static static static x)

println("")
local y = 222
for (local x = y; x < 224; x++) {
  println(static static ~(static static static ~x + static(x * 2) + static x))
  println(~(~y + (y * 2) + y))
}

