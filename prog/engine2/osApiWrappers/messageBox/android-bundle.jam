# to be included with osApiWrappers

if $(Platform) in android {
   BundleCopy +=
      @$(Root)/prog/engine2/osApiWrappers/messageBox/MessageBoxWrapper.java      src/com/gaijinent/MessageBox/MessageBoxWrapper.java
   ;
   UseAndroidNativeMessageBox = yes ;
}
