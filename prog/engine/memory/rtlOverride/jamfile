Root    ?= ../../../.. ;
Location = prog/engine/memory/rtlOverride ;

TargetType  = lib ;
Target      = engine/memory/rtlOverride.lib ;

AddIncludes = $(Root)/prog/3rdPartyLibs/memAllocators ;

Sources =
  stubRtl.cpp
;

CPPopt = -DUSE_RTL_ALLOC ;
if $(KernelLinkage) != dynamic { Exit $(Target) doesn't support KernelLinkage=$(KernelLinkage:E=static) ; }

include $(Root)/prog/_jBuild/build.jam ;
