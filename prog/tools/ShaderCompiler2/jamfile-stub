Root    ?= ../../.. ;
Location = prog/tools/ShaderCompiler2 ;

include $(Root)/$(Location)/jamfile-common ;

Target = tools/dsc2-stub ;

CPPopt += -D_CROSS_TARGET_EMPTY -DBINDUMP_TARGET_BE=0 ;

if $(Platform) = macOS { CPPopt += -Wno-switch ; }

include $(Root)/prog/_jBuild/build.jam ;
