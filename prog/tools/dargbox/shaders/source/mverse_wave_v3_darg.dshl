include "darg_shader_inc.dshl"

shader mverse_wave_v3
{
  DARG_PIXEL_SHADER_SETUP()

  hlsl(ps) {
    #include "mverse_wave_v3_darg.hlsl"

    float4 main_ps(VsOutput input) : SV_Target {
      return darg_ps(input.texcoord);
    }
  }
  compile("target_ps", "main_ps");
}
