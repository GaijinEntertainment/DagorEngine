zlib-ng is a fork of zlib with performance improvements for modern CPUs
(SIMD/hardware-accelerated paths), while keeping API/ABI compatibility
with zlib.

Upstream: zlib-ng, https://github.com/zlib-ng/zlib-ng
Vendored version: 2.1.0.devel (see ZLIBNG_VERSION in zlib-ng.h)
