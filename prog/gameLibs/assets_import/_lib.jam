
if $(HaveRenderer) = yes
{
  if $(Platform) in windows && $(HaveAssetManager) = yes && $(Config) != rel {
    gamePulls += gameLibs_assets_import_DAS_pull_AOT ;
    gamePulls += AssetsImportModule_pull ;
  }
  gamePulls += framework_assets_import_pull ;
  UseProgLibs += gameLibs/assets_import ;
}